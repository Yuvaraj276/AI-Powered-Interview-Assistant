{
  "name": "ai-powered-interview-assistant",
  "version": "1.0.0",
  "description": "An AI-powered interview assistant with real-time transcription, question generation, and candidate evaluation",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-deps": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "keywords": ["ai", "interview", "assistant", "transcription", "react", "nodejs"],
  "author": "AI-Powered Interview Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}